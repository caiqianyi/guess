<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.caiqianyi.guess.caipiao.core.dao.IJCLQMatchMapper">

	<sql id="Base_Column_List">
		id, seq, league, hostTeam, gustTeam, score, status, rf,
		dxf, matchTime, endTime, createTime
	</sql>

	<select id="findAllMatchByTime" resultType="com.caiqianyi.guess.jclq.entity.JCLQMatch">
		select
		<include refid="Base_Column_List" />
		from jclq_match where league = #{league}
		and DATE_FORMAT(matchTime,'%Y%m%d') = #{day}
		<if test="status != null">
			and status = #{status}
		</if>
		order by matchTime
	</select>
	
	<select id="findMatch" resultType="com.caiqianyi.guess.jclq.entity.JCLQMatch">
		select
		<include refid="Base_Column_List" />
		from jclq_match where seq = #{seq}
	</select>

	<insert id="insert" parameterType="com.caiqianyi.guess.jclq.entity.JCLQMatch">
		insert into jclq_match (
		seq, matchId, league, hostTeam, gustTeam, score, status, rf, dxf, matchTime,
		endTime
		)
		values (#{seq}, #{matchId}, #{league}, #{hostTeam}, #{gustTeam}, #{score},
		#{status}, #{rf}, #{dxf}, #{matchTime}, #{endTime})
	</insert>

	<update id="update" parameterType="com.caiqianyi.guess.jclq.entity.JCLQMatch">
		update jclq_match
		<set>
			<if test="score != null">
				score = #{score},
			</if>
			<if test="status != null">
				status = #{status},
			</if>
			<if test="dxf != null">
				dxf = #{dxf},
			</if>
			<if test="rf != null">
				rf = #{rf}
			</if>
		</set>
		where id = #{id}
	</update>
	
</mapper>
